/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['raw.githubusercontent.com'], // Adicione o domínio da origem aqui
    },
}

module.exports = nextConfig
